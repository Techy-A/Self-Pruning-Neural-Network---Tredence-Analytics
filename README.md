# Self-Pruning-Neural-Network---Tredence-Analytics
A PyTorch implementation of a self-pruning neural network on CIFAR-10. Each weight carries a learnable gate that the optimizer drives toward zero during training, producing a sparse model without any separate pruning step. Submitted as the Tredence AI Engineering Internship case study.
